/**
 * Root entrypoint for direct directory discovery. Re-exports the
 * unified package entry; holds no logic. The npm/Git package entry
 * is `./src/index.ts` (see `main` in package.json).
 */

export { default } from "./src/index.ts";
